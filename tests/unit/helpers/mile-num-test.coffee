`import { mileNum } from '../../../helpers/mile-num'`
`import { module, test } from 'qunit'`

module 'MileNumHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = mileNum 42
  assert.ok result
